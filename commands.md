# コマンド

## cd

今自分がいるフォルダ（カレントディレクトリ）を移動する
下記は、ホームフォルダ以下の、デスクトップフォルダ以下の、sampleフォルダに移動する例です。

```sh
cd ~/デスクトップ/サムライ/sample
```

## ls

今自分がいるフォルダ（カレントディレクトリ）以下の、ファイルやフォルダを表示する

## cat

引数で指定したテキストファイルの中身を出力する

 cat ample.txt

###git

### git status

Gitリポジトリの現在の状態（作業ディレクトリとステージングエリア）を表示するコマンド

###git diff

ワークツリーとインデックスまたはツリー間の変更、インデックスとツリー間の変更、2つのツリー間の変更、マージによる変更、2つのブロブオブジェクト間の変更、またはディスク上の2つのファイル間の変更を表示します。

###git commit

リポジトリへの変更を記録
インデックスの現在の内容と変更を説明する指定されたログメッセージを含む新しいコミットを作成します。新しいコミットはヘッドの直接のチャイルドであり、通常は現在のブランチの末端であり、ブランチはそれを指すように更新されます（ブランチがワークツリーに関連付けられていない場合を除きます。この場合、ヘッドはgit-checkout[1]で説明されているように「detached」とされます）

コミットとは
commit（コミット）」とは、データや変更内容を確定・保存する操作です。Gitなどのバージョン管理システムではファイル変更を履歴として記録すること、データベースでは一連の処理（トランザクション）を完了させることを指し、作業の区切りとして用いられます


###git config
git configは、Gitの動作やユーザー情報を設定・管理するコマンドです。ユーザー名、メールアドレス、エディタ、エイリアスなどを設定でき、スコープ（システム、グローバル、ローカル）の3段階で、ローカル>グローバル>システムの順に優先されます

###git push

分散型バージョン管理システム「Git」において、ローカルリポジトリ（自分のPC）で行った変更やコミット履歴を、リモートリポジトリ（GitHubやGitLabなどの共有サーバー）に送信・反映させるコマンド


###git branch

Gitなどの分散型バージョン管理システムにおいて、履歴の流れを分岐して記録していくための機能


###git merge feature

マージ (Merge) は、同じレイヤー上の 2 つ以上のライン フィーチャまたはポリゴン フィーチャを 1 つのフィーチャに結合します

###git checkout develop

Gitというバージョン管理システムにおいて、現在作業しているブランチから「develop（開発用）ブランチ」へ切り替えるためのコマンド


###git restore

Gitで作業ツリー（現在編集中のファイル）やステージングエリア（コミット準備中のファイル）の変更を、安全かつ直感的に以前のコミット状態に戻す（復元する）ためのコマンド

###git reset

Gitでコミットした内容やステージングした内容を取り消し、ブランチのHEAD（最新状態）を過去の特定のコミットまで戻すコマンド

###git revert

Gitにおいて過去の特定のコミット（変更）を打ち消す新しいコミットを作成するコマンド


###git add sample

バージョン管理システム「Git」において、sample という名前のファイル（またはフォルダ）の変更内容を、次回の記録（コミット）の対象として登録するコマンド

  
